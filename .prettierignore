# Build outputs
.next/
out/
dist/
build/

# Generated files
public/r/
public/_pagefind/
next-env.d.ts

# Dependencies
node_modules/

# Package manager
pnpm-lock.yaml

# Environment files
.env*

# Local Claude Code permission allowlists — per-developer, not part of
# the shared config. Committed template is `.claude/settings.json`.
.claude/settings.local.json

# Supabase
supabase/

# Registry JSON (generated by pnpm registry:build)
public/r/**

# The committed registry snapshot. `pnpm registry:sync` emits it as
# `JSON.stringify(payload, null, 2)`, and `registry:verify` compares that output
# byte-for-byte against the file. Prettier reformats it differently, so with
# both active `format:check` and `registry:verify` cannot pass at the same time.
# It is generated and never hand-edited (CLAUDE.md §6.1), so the generator owns
# its formatting — the same reasoning as `components/ui/` below.
registry.json

# Skill MDX bodies — canonical content lives in the Supabase `skills`
# table; these files are a published snapshot regenerated by
# `pnpm skills:sync` and written to BOTH locations below. Prettier-
# formatting them silently introduces drift (escaped underscores in
# code-adjacent prose, JSX reflows) and breaks `pnpm skills:verify`'s
# hash check. Leave them as-is.
packages/design-agent-skills/skills/
.claude/skills/*.md
!.claude/skills/README.md

# Registry component snapshots — components/ui/*.tsx are generated from the
# Supabase `components` source_code by `pnpm registry:sync` (see CLAUDE.md 8.4)
# and overwritten on every sync. Prettier-formatting them drifts them from the
# generated output and breaks `registry:verify`; leave them as the sync emits.
components/ui/

# Misc
*.min.js
*.min.css
